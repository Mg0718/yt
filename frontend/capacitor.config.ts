import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ytdownloader.app',
  appName: 'yt-playlist-downloader',
  webDir: 'dist'
};

export default config;
